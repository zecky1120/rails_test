<h1 class="mb-3">ユーザー一覧</h1>

<% @users.each do | user | %>
<img width="60" height="auto" src="<%= "/user_images/#{user.image_name}" %>">
<p><%= link_to(user.name,"/users/#{user.id}") %></p>
<hr>
<% end %>
